# CinNova Conversion Map — Phase 11.2

Active zero-budget path for The Southeast Asian Table.

## Funnel

```
Organic / direct / social / Products / Blog (manual module)
        ↓
  /books  (featured AVAILABLE hierarchy)
        ↓
  /books/the-southeast-asian-table
        ↓
  View on Amazon (hero | mid_page | footer)
        ↓
  Amazon Kindle outbound  (measurable; purchase completion not known to CinNova)
```

Parallel lead path:

```
Books / SEAT / Beyond / Products / Blog / News
        ↓
  Newsletter (placement + entity attribution)
        ↓
  Future launches & publishing updates
```

## Discovery surfaces (qualified only)

| Surface | Path to SEAT | Notes |
|---|---|---|
| `/books` | Featured + catalog AVAILABLE card | Primary commercial hierarchy |
| SEAT detail | Direct | Optimized CTAs |
| `/products` Books & Publishing | Books + SEAT detail | Software vs publishing separation |
| Homepage | Explore CinNova Books | Single tasteful strip — not a store |
| Blog (manual `commercialModule`) | SEAT or newsletter | Never auto-random |
| News | Newsletter only | No cookbook CTAs in news |

## Placement analytics

SEAT detail Amazon CTAs use:

- `placement=hero`
- `placement=mid_page`
- `placement=footer`

All share the same verified URL: `https://www.amazon.com/dp/B0H8YL3L5L`

## Important

Amazon outbound clicks are measurable. Amazon completed purchases are **not** visible to CinNova unless external reporting/integration is added later. Do not fire `purchase`.
